import { Router } from 'express'

const router = Router()

// son
// router.use('/son', async (req, res, next) =>
//     (await import('./son.js')).default(req, res, next)
// )

export default router
